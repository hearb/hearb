box: golang

build:
  steps:
      - script:
        name: go get
        code: |
          go get -d -t -v ./server/...

      - script:
        name: go test
        code: |
          go test -v ./server/...

      - script:
        name: go build
        code: |
          go build -v ./server/hearb.go
